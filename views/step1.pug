extends layout.pug

block content
  p.lead.
    Je souhaite voter pour Jean-Luc Mélenchon, mais je ne serai pas dans ma commune d'inscription le 23 avril&nbsp;?
  p.lead.
    En trois clics, je trouve quelqu'un qui peut prendre ma procuration.
  form(action="/etape-1" method="POST")
    .form-group
      label 1. J'indique mon adresse e-mail.
      input.form-control.input-block.input-lg.text-center(type="email" name="email" required)
    .form-group
      input.btn.btn-primary.pull-right(type="submit" value="Trouver quelqu'un dans ma commune")
